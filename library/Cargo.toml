[package]
name = "bencode_lib"
version = "0.1.7"
edition = "2024"
description = "Bencode library implementation."
license = "MIT"

[features]
default = ["std", "json", "toml", "xml", "yaml"]
std = []
# Optional format conversion features (reduce binary size)
json = []
toml = []
xml = []
yaml = []

[dependencies]
rand = { version = "0.9.2", optional = true }
